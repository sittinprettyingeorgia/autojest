import { screen, render } from '@testing-library/react';
import { TestApp } from './parser.test';

describe('testing TestApp component', () => {
  it('should render all visible elements', async () => {
    /*render(<TestApp />);
    expect(
      await screen.findByText('This is div1 paragraph')
    ).toBeInTheDocument();
    expect(await screen.findByText('This is a span1')).toBeInTheDocument();
    expect(await screen.findByText('change state1')).toBeInTheDocument();
    expect(
      await screen.findByText('This is div2 paragraph.')
    ).toBeInTheDocument();
    expect(await screen.findByText('test1')).toBeInTheDocument();
    expect(await screen.findByText('test2')).toBeInTheDocument();
    expect(await screen.findByText('outsideButton')).toBeInTheDocument();
    expect(
      await screen.findByPlaceholderText('test placeholder1')
    ).toBeInTheDocument();
    for (const item of await screen.findAllByPlaceholderText(
      'test placeholder2'
    )) {
      expect(item).toBeInTheDocument();
    }
    expect(await screen.findByAltText('img1')).toBeInTheDocument();
    for (const item of await screen.findAllByAltText('img2')) {
      expect(item).toBeInTheDocument();
    }
    expect(await screen.findByAltText('img3')).toBeInTheDocument();*/
  });
  /*describe('should handle onClick events', () => {
    it('should respond to onclick', async () => {
      render(<TestApp />);
    });
  });*/
});
