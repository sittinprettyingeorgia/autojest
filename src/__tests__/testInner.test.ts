import { handleFirstOpeningBracket } from 'testInner';
import { Attribute } from 'types';
import {
  DONT_KEEP_REGEX,
  CHILDREN_KEY,
  MULTIPLE_CHILD,
  SINGLE_CHILD,
  MULTI,
  SINGLE,
  DONT_KEEP_MAP,
  MATCH_TEXT_CHILD,
} from 'constant';

describe('Testing inner functions', () => {
  let attr: Attribute;

  beforeEach(() => {
    attr = {};
  });

  describe('testing handleFirstOpeningBracket', () => {});
});
